import 'package:flutter/material.dart';

class AppRadius {
  static final small = BorderRadius.all(Radius.circular(4.0));
  // static final medium = BorderRadius.circular(8.0);
  // static final large = BorderRadius.circular(12.0);
}
